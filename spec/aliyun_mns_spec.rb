require 'spec_helper'

describe AliyunMns do
  it 'has a version number' do
    expect(AliyunMns::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
